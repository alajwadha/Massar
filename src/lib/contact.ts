// Where buyers send their CV after paying (manual onboarding model).
// Set `whatsapp` to a full international number, digits only, no plus or spaces,
// e.g. '9665XXXXXXXX' to show the WhatsApp button. Leave it '' to hide it.
// `email` always shows.
export const CONTACT = {
  email: 'alialajwad951@gmail.com',
  whatsapp: '',
};
